# Machine-local secrets/paths — copy .env.example to .env instead
.env

# Completion cache (regenerated by compinit on first shell)
.zcompdump
.zcompdump.zwc
zcompdump*

# Plugins are installed on demand by plugins.zsh
plugins/
